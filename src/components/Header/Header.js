import React from 'react'
import styles from './Header.styles'

const Header = () => (
    <nav style={styles.wrapper}>
        <h1 style={styles.header}>Freudle</h1>
    </nav>
)

export default Header